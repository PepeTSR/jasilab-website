{
  "name": "jasilab-website",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev --host 0.0.0.0 --port 4321",
    "build": "astro build",
    "build:local": "npm run sync:docs && astro build",
    "deploy": "wrangler deploy",
    "preview": "astro preview --host 0.0.0.0 --port 4321",
    "sync:docs": "node scripts/sync-cvt-docs.mjs",
    "astro": "astro"
  },
  "dependencies": {
    "@tailwindcss/vite": "^4.3.0",
    "astro": "^6.4.4",
    "tailwindcss": "^4.3.0"
  },
  "devDependencies": {
    "wrangler": "^4.98.0"
  }
}
