---
import { cvtAppUrl } from "../lib/nav";
import { cvtMarketingUrl } from "../lib/site";
---

<footer class="border-t border-hairline bg-canvas">
  <div class="mx-auto max-w-6xl px-4 py-8 sm:px-8">
    <p class="text-sm text-mute">
      CVT public site: <a href={cvtMarketingUrl} class="text-primary-deep hover:text-primary">{cvtMarketingUrl.replace("https://", "")}</a>.
      The live registry is at{" "}
      <a href={cvtAppUrl} class="text-primary-deep hover:text-primary" target="_blank" rel="noopener">cvt.jasilab.net</a>.
    </p>
  </div>
</footer>
